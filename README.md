# vesselproserpina.com

Public website for **Vessel Proserpina**. Static, single-file, zero dependencies.

- `index.html` — the site
- `brand/` — brand assets (Discord server icon + its HTML source; re-render via headless Edge screenshot at 512x512)
- Deployed via GitHub Pages, custom domain `vesselproserpina.com`
